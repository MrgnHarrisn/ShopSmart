name: hello_world

on:
  push:
    branches:
      - main
  pull_requests:
    branches:
    - main

jobs:
  hello_job:
    runs-on:
      windows-latest

    steps:
    - name: Hello step
      run: echo "Hello World"